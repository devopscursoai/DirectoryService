Decoding the ls -l output
ls -l <-rw-rw-- i jason>
to see the hidden files you need to run the command 
<ls -a or ls -l -a or ls -al> if you run ls -l you will not be able see the hidden files

to listing the files by types 
use < ls -F >

if you wanna list files by time and in reverse
ls -t list files by time
ls -r reverse order
ls -latr long listing including all files reverse sorted by time

listing file with color
<ls --color> colorize the output

working with spaces in names, just say no to spaces you can use alternative like
Hyphens (-)
Underscores(_)
camelcase
uses backslash (\)
use quotes ("my note.txt")

PERMISSION       file                                Directory
r    read>>> allows file to be read               allows file names in the directory to be read
w    write    allows a file to modified            allows entries to be modified within the directory
x    execute.  allows the execution of a file      allows access to contents and metadata for entries

This persmission can be apply to all the user, group, other and all.

Changing permissions
Item                                Meaning
chmod                               change mode command 
ugoa                                user catergory userr. group, other and all
+-=                                 Add, subtract, or set permission
rwx                                 read, write,e\ execute
Add write permission<<chmod g+w (example of the user or group) 

FIND OPTIONS
mtime days    finds files that are days old.
size mum      finds file that are of size num
newer file    finds files that are newer than file

-exec command 
